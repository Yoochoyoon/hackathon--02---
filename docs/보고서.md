# 해커톤 결과 보고서

> **작성 안내 (제출 전 이 블록은 지운다)**
> - 채운 곳과 못 채운 곳을 `[ ]`로 표시해 뒀다. 전부 없앤 뒤 PDF로 바꾼다.
> - **3장은 `작업기록.md`에서 옮겨 온다. 기억으로 쓰지 않는다.**
> - 분량 2~4쪽. PDF 변환 후 쪽수를 반드시 확인한다.
>
> **서식 규칙 (한국 보고서 실무 관행)**
> - 항목 기호는 `□`(대분류) → `○`(중분류) → `―`(소분류) → `※`(참조) 순으로 쓴다.
>   `1. 가. 1)`은 공문서 번호 체계라 보고서에는 쓰지 않는다.
> - 날짜는 `2026. 8. 5.` 로 쓴다 (월·일 앞 0 생략, 마지막 마침표 포함).
> - 도표·흐름은 **Mermaid**로 그린다. 아스키 아트를 쓰지 않는다.

---

## 표지

| 항목 | 내용 |
|---|---|
| 팀번호 / 팀이름 | 02 / `[ ]` |
| 팀원 및 역할 | 유초윤(Yoochoyoon) — 필수기능(`app.js`) · 신찬영(scyscu153-byte) — 차별화기능(`style.css`·`features.js`) |
| GitHub 저장소 URL | https://github.com/Yoochoyoon/hackathon--02--- |
| 제출일 | 2026. 8. 5. |

---

## 1. 문제 정의와 기획

□ 이 웹앱이 해결하려는 문제

  동아리 활동 기록은 보통 단톡방 공지나 개인 메모에 흩어진다. 학기가 끝나고 활동
내역을 정리할 때가 되면 어떤 활동을 언제 몇 명이 했는지 다시 모아야 한다.
이 앱은 활동을 등록하는 시점에 한곳에 쌓아 두어, 나중에 찾아 헤매지 않게 한다.

□ 주 사용자와 사용 상황

  동아리 임원. 활동이 끝난 직후 휴대폰이나 노트북으로 그 자리에서 한 건을 등록한다.
학기 말에는 쌓인 목록을 훑어보며 정리한다. 즉 **등록은 짧고 자주, 조회는 몰아서**
일어난다.

□ 구현한 필수 기능 3가지

  ○ 활동 등록 — 활동명·날짜·장소·참여 인원·메모를 입력해 저장
  ○ 활동 목록 조회 — 최신순 표시, 빈 목록일 때 안내 문구
  ○ 활동 삭제 — 개별 삭제, 삭제 전 확인 절차

□ 구현한 선택 기능과 선택한 이유

`[ 계획(계획.md)은 검색 + 반응형이었으나, 두 사람 합의로 최종 범위가 아래처럼 늘어남 ]`

  ○ 키워드 검색: 학기 말에 목록이 길어지면 스크롤로 찾기 어렵다. 등록은 짧고 조회는
  몰아서 한다는 사용 상황에 검색이 직접 맞물린다.
  ○ 반응형: 활동 직후 그 자리에서 등록하는 상황이라 휴대폰 화면을 전제해야 한다.
  ○ 월별 통계 차트: 검색·반응형에 더해 활동 빈도를 한눈에 보여주는 차별화 기능으로
  추가. 외부 차트 라이브러리 없이 `<canvas>`로 직접 그려서(신찬영 담당, `features.js`)
  CDN 의존 없이 오프라인에서도 동작하게 했다.

□ 필수 담당 쪽에 추가로 배정한 기능 (원래 선택 후보였으나 CRUD 로직 연장선이라 이동)

  ○ 수정 기능, 기간 필터
  ○ 활동 유형 태그(`category`), 참여자 명단(`attendees`), 회비 사용 금액(`cost`) 기록
  + 목록 하단 합계 표시
  ○ 같은 날짜에 같은 활동명이 이미 있으면 등록 전 한 번 더 확인하는 중복 등록 경고

---

## 2. 설계

□ 화면 구성

```mermaid
flowchart TB
    subgraph page["활동 기록 화면 (단일 페이지)"]
        direction TB
        H["제목"]
        F["활동 등록 폼<br/>활동명 · 날짜 · 장소 · 참여 인원 · 메모 · [등록]"]
        S["검색창<br/>활동명 · 장소 키워드"]
        L["활동 목록 (최신순)<br/>활동명 (날짜) · 장소 · N명 · 메모 · [삭제]"]
        H --> F --> S --> L
    end
```

한 화면에 등록과 목록을 같이 뒀다. 활동이 끝난 직후 바로 적는 상황이라
화면을 옮겨 다니게 하면 등록이 번거로워진다.

□ 등록 처리 흐름

```mermaid
flowchart TD
    A["등록 버튼"] --> B{활동명이 있나}
    B -->|없음| E["메시지 표시<br/>저장 안 함"]
    B -->|있음| C{날짜가 미래인가}
    C -->|미래| E
    C -->|아님| D{참여 인원이<br/>1 이상 정수인가}
    D -->|아님| E
    D -->|맞음| G["localStorage 에 추가<br/>createdAt 기록"]
    G --> H["목록 다시 그리기"]
```

검증 세 가지를 모두 통과해야 저장한다. 어느 하나라도 걸리면 저장하지 않고
무엇이 잘못됐는지 화면에 알린다.

□ 데이터 구조

`localStorage`의 `"activities"` 키에 배열로 저장한다.

| 필드 | 뜻 |
|---|---|
| `id` | 활동 식별자. 저장 시각 기반. 삭제할 때 이 값으로 찾는다 |
| `title` | 활동명 (필수) |
| `date` | 활동 날짜. 미래 날짜 불가 (필수) |
| `place` | 장소 |
| `memberCount` | 참여 인원. 1 이상 정수 (필수) |
| `category` | 활동 유형: 정기모임 · 행사 · 봉사 · 기타 |
| `attendees` | 참여자 명단 (선택 입력, 쉼표 구분 → 배열로 저장) |
| `cost` | 활동에 쓴 회비 금액, 원 단위 (선택, 기본 0) |
| `memo` | 메모 |
| `createdAt` | 등록 시각. 최신순 정렬 기준 |

정렬을 `date`가 아니라 `createdAt`으로 하는 이유: 지난 활동을 뒤늦게 입력하는
경우가 있어, 활동 날짜순으로 세우면 방금 넣은 항목이 목록 중간에 묻힌다.

□ 파일 구조

| 파일 | 역할 |
|---|---|
| `index.html` | 화면 구조 |
| `style.css` | 스타일·반응형 (신찬영) |
| `app.js` | 필수 CRUD·수정·기간필터·검증 로직 (유초윤) |
| `features.js` | 검색·월별 통계 차트, `<canvas>` 직접 구현 (신찬영) |
| `CLAUDE.md` | Claude Code 작업 규칙 |

---

## 3. Claude Code 활용 과정

### 3-1. 작성한 CLAUDE.md

□ 어떤 규칙을 넣었고, 왜 필요하다고 판단했는지

`[ 작업 후 채움 — 아래는 지금까지 확정된 것 ]`

  ○ **"한 번에 하나의 기능만 구현한다"** — 여러 기능을 한꺼번에 시키면 어디서 틀어졌는지
  못 찾는다. 기능 단위로 커밋하라는 과제 규칙과도 맞는다.
  ○ **"기존에 동작하던 코드를 요청 없이 수정하지 않는다"** — 되던 것이 깨지면 되돌리는
  데 시간을 뺏긴다. 3시간짜리 작업에서 가장 큰 위험이다.
  ○ **"확실하지 않은 부분은 추측하지 말고 먼저 질문한다"** — 추측으로 채운 코드는
  나중에 찾기 어렵다.
  ○ **"기록 규칙"(템플릿에 없는 항목을 직접 추가)** — 이 보고서 3장이 실제 입력한
  프롬프트 원문을 요구한다. 나중에 기억으로 복원하면 틀린 내용이 되므로,
  작업하면서 `docs/작업기록.md`에 그때그때 남기도록 규칙에 넣었다.

□ 작업 도중 규칙을 수정했다면 무엇을 왜 바꿨는지

`[ ] ← 수정할 때마다 작업기록.md 5번 표에 적어두고 여기로 옮긴다`

### 3-2. 구현 전 계획

□ 계획 화면 캡처

`[ docs/계획.md 를 화면 캡처해 삽입 ]`

□ 실제 작업이 계획과 달라진 부분

`[ ] ← 작업기록.md 4번에서 옮겨온다`

### 3-3. 핵심 프롬프트 3건

> **실제 입력한 문장 그대로 옮긴다. 요약하지 않는다.**

**① 최초 요청 — 역할 분담 계획**

| 구분 | 내용 |
|---|---|
| 무엇을 요청했나 | `https://github.com/sung2ne/20260805 이 양식에 맞춰서 제작해야하는데 지금 기획은 다른 쪽에서 진행중에 있고, 어떻게 진행할지 계획이 필요해. 팀은 총 2명이서 진행할 예정이고 둘의 역할을 어떻게 분담해서 가장 효율적이게 할 수 있는지를 해야해. 둘다 클로드 코드를 이용할 예정이야.` |
| 어떤 결과를 받았나 | 양식 저장소의 요구사항 명세서·CLAUDE 템플릿·보고서 양식을 확인하고, 2인 역할 분담 실행계획 문서를 작성함 |
| 만족스러웠는지, 아니라면 왜 | 만족. 이어서 CLAUDE.md 작성으로 자연스럽게 이어졌다 |

**② 디자인 재구성 요청 — 애매한 점 확인**

| 구분 | 내용 |
|---|---|
| 무엇을 요청했나 | `재구성해줘. 비주얼 디자인은 https://getdesign.md/apple/design-md 이거로 진행 할 수 있어? 만약 충돌되거나 애매한 사항이 있으면 물어봐줘.` |
| 어떤 결과를 받았나 | 레이아웃 재구성을 진행하다가, 실제 디자인 스펙이 검증 안 된 npm 패키지 실행(`npx`)으로만 얻어진다는 걸 발견해 먼저 확인 질문을 던짐 (아래 3-4 참고) |
| 만족스러웠는지, 아니라면 왜 | 만족. "애매하면 물어봐달라"는 지시를 정확히 지켰다 |

**③ 디자인 방향 전환 — Apple → LINE**

| 구분 | 내용 |
|---|---|
| 무엇을 요청했나 | `아 이 디자인 아닌거 같아 https://www.designmd.co/d/naver-com 이걸로 해줄 수 있어? 좀더 한눈에 다 보일 수 있었음해.` |
| 어떤 결과를 받았나 | Apple 스펙(히어로+명암 교차 풀블리드 타일)이 폼+목록 유틸리티 앱에 안 맞다고 보고 폐기, LINE Design System(LDSG) 토큰으로 전환 진행 중 |
| 만족스러웠는지, 아니라면 왜 | 방향 전환은 받아들여짐. 이 시점까지는 브라우저 검증 전이라 최종 만족 여부는 `[ 진행 중 — 검증 후 채움 ]` |

### 3-4. 프롬프트 개선 사례 1건

- 처음 요청한 내용: "비주얼 디자인은 https://getdesign.md/apple/design-md 이거로 진행 할 수 있어?"
- 결과가 기대와 달랐던 지점: 페이지 자체에는 색상·폰트 같은 실제 스펙 값이 공개돼 있지 않고, 검증 안 된 제3자 npm 패키지(`npx getdesign@latest add apple`)를 실행해야만 받을 수 있었다.
- 요청을 어떻게 고쳤는지: 사용자에게 "검증 안 된 npm 패키지를 임의 실행하는 게 걸린다"고 직접 알리고, 사용자 본인이 터미널에서 직접 실행하도록 요청했다.
- 고친 뒤 무엇이 달라졌는지: 신뢰할 수 없는 코드를 대신 실행하지 않고도 사용자가 승인한 방식으로 안전하게 스펙을 확보했다.

### 3-5. 오류 해결 사례 1건

- 발생한 오류 (메시지 또는 증상): 활동명을 비우거나 참여 인원을 0으로 입력해 등록을 시도했을 때, 커스텀 검증 메시지가 뜨지 않고 이전 메시지가 그대로 남아있거나 아무 반응이 없었다.
- 원인 파악 과정: 브라우저에서 폼 제출을 반복 테스트한 결과, `<input required>`·`<input min="1">` 같은 HTML5 기본 검증 속성이 우리 JS의 `validate()` 함수보다 먼저 개입해 `submit` 이벤트 자체가 발생하지 않는 것을 확인했다.
- 해결 방법: `<form id="activityForm">`에 `novalidate` 속성을 추가해 네이티브 검증을 끄고, 항상 우리 JS의 `validate()` 로직만 타도록 수정했다.
- 이 과정에서 배운 점: HTML5 기본 검증 속성과 커스텀 JS 검증을 같은 필드에 함께 쓰면 브라우저가 먼저 개입해 커스텀 로직이 실행되지 않을 수 있다. 커스텀 검증 메시지를 쓰려면 `novalidate`를 명시적으로 켜야 한다.

---

## 4. 실행 결과

□ 주요 화면 스크린샷

`[ 2장 이상. 글자가 읽히는 크기로. 각 화면 설명을 붙인다 ]`

□ 테스트한 항목과 결과

| 항목 | 확인 내용 | 결과 |
|---|---|---|
| 활동 등록 | localStorage에 정상 저장되는가 | ✅ 확인 (브라우저에서 직접 등록 후 저장값 확인) |
| 목록 조회 | 최신순으로 표시되는가 | ✅ 확인 (`createdAt` 기준 정렬 확인) |
| 빈 목록 | 안내 문구가 뜨는가 | ✅ 확인 |
| 수정·기간 필터·중복 등록 확인 | 정상 동작하는가 | ✅ 확인 (수정 시 폼에 값 채워짐, 기간 배제 케이스, 중복 등록 취소/승인 모두 테스트) |
| 삭제 | 확인 후 삭제되는가 | ✅ 확인. 단, "취소하면 안 지워지는가"는 중복 등록 경고(동일 confirm 패턴)로만 검증했고 삭제 자체의 취소 케이스는 `[ 미확인 ]` |
| 활동명 검증 | 비우면 저장이 막히는가 | ✅ 확인 |
| 날짜 검증 | 미래 날짜가 막히는가 | ✅ 확인 |
| 인원 검증 | 0이면 막히는가 | ✅ 확인. 소수 입력 케이스는 `[ 미확인 ]` |
| 검색·통계 차트 연동 | 등록/삭제 후 검색·차트에 즉시 반영되는가 | ✅ 확인 (`window.Features.refresh()` 연동) |
| 비주얼 디자인(LINE 토큰 적용) | 화면에서 의도대로 보이는가 | `[ 진행 중 — 브라우저 검증 전 ]` |

---

## 5. 한계와 개선 방향

□ 시간 내 구현하지 못한 것: `[ ]`
□ 현재 코드의 문제점이나 불안한 부분: `[ ]`
□ 시간이 더 있었다면 무엇을 했을지: `[ ]`

---

## 6. 역할 분담과 소감

□ 팀원별 담당 업무

| 팀원 | 담당 |
|---|---|
| 유초윤 (Yoochoyoon) | `app.js` — 필수 CRUD·수정·기간필터·검증·태그/참여자명단/회비 로직, 테스트 |
| 신찬영 (scyscu153-byte) | `style.css`·`features.js` — 화면·검색·통계 차트, 기록·보고서 |

□ AI를 활용한 개발에 대해 느낀 점

- `[ 팀원 1 — 한두 문장 ]`
- `[ 팀원 2 — 한두 문장 ]`
