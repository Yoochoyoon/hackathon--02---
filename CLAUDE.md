# CLAUDE.md — 동아리 활동 기록 관리 웹앱

## 1. 프로젝트 개요

- **팀명**: 02 시너지
- **팀원**:
  - 유초윤 (GitHub: `Yoochoyoon`) — **필수기능 담당**
  - 신찬영 (GitHub: `scyscu153-byte`) — **차별화기능 담당**
- **저장소**: https://github.com/Yoochoyoon/hackathon--02--cho.git
- **프로젝트 설명**: 동아리 활동(모임/행사) 이력을 등록·조회·삭제·수정하고, 검색/필터/통계로 한눈에 파악할 수 있는 단일 페이지 웹앱

## 2. 기술 스택

- 프론트엔드: HTML / CSS / JavaScript (프레임워크 없음)
- 데이터 저장: 브라우저 `localStorage`
- 외부 라이브러리: 사용하지 않음 (통계 차트는 `<canvas>` 또는 CSS 막대로 직접 구현 — 오프라인/로딩 지연 리스크 방지)
- 개발 도구: Claude Code

## 3. 역할 분담 & 파일 구조

| 파일 | 소유자 | 내용 |
|---|---|---|
| `index.html` | 공동(최초 10분 동기화 시 함께 스켈레톤 작성) | 필수기능 영역(`#app`, `#activityForm`, `#activityList`)과 차별화기능 영역(`#searchBox`, `#filterControls`, `#statsChart`) 컨테이너를 미리 빈 채로 배치. 이후 각자 자기 영역 안쪽만 채움 |
| `app.js` | **유초윤** | 핵심 CRUD(`addActivity`, `renderList`, `deleteActivity`, `updateActivity`), 입력값 검증, 기간 필터 로직, localStorage 저장/로드 |
| `style.css` | **신찬영** | 기본 스타일 + 반응형 미디어쿼리 |
| `features.js` | **신찬영** | 검색 기능, 통계 차트. `app.js`를 직접 수정하지 않고 동일 localStorage 키를 읽어서 동작 |

### 기능 분담

- **유초윤 (필수기능)**
  1. 활동 등록 (활동명, 날짜, 장소, 참여 인원, 메모)
  2. 최신순 목록 조회
  3. 삭제 기능 (확인 절차 포함)
  4. 입력값 검증 (활동명 필수 / 날짜 미래 입력 불가 / 참여 인원 1 이상 정수)
  5. 수정 기능
  6. 기간 필터

- **신찬영 (차별화기능)**
  1. 반응형 레이아웃
  2. 검색 기능
  3. 통계 차트

## 4. 데이터 구조

```js
{
  id: string,           // 고유 식별자
  title: string,        // 활동명 (필수)
  date: string,          // YYYY-MM-DD, 미래 날짜 불가 (필수)
  place: string,         // 장소
  memberCount: number,   // 참여 인원, 1 이상 정수 (필수)
  category: string,      // 활동 유형: 정기모임 | 행사 | 봉사 | 기타
  attendees: string[],   // 참여자 명단 (선택 입력, 쉼표로 구분해 배열로 저장)
  cost: number,          // 활동에 사용한 회비 금액, 원 단위 (선택, 기본 0)
  memo: string,           // 메모
  createdAt: string      // 등록 시각 (ISO)
}
```

> 2026-08-05 14:50경 추가: 태그(category)·참여자 명단(attendees)·회비(cost) 필드, 같은 날짜+활동명 중복 등록 시 확인창. 기존 `title`/`date`/`memberCount`/`place`/`memo` 필드는 그대로라 features.js·style.css는 수정 없이도 동작함.

## 5. 코딩 규칙

- 변수/함수명: camelCase
- 주석: 한글로 작성
- 단일 책임 원칙 준수 (함수 하나는 한 가지 일만)
- 들여쓰기: 2칸

## 6. 작업 방식

- **브랜치 전략**: `feat/core`(유초윤), `feat/ui`(신찬영)에서 각자 작업 후 `main`으로 병합
- **병합 시점**: 구현 시작 후 70분 지점(1차), 115분 지점(2차 최종) — 그 외 구간은 병합하지 않고 각자 브랜치에서만 작업
- 작업 전 계획 먼저 세우고 단계적으로 구현
- 기존 코드는 보존하고, 변경이 필요하면 먼저 확인 후 수정
- 확신 없는 부분은 임의로 판단하지 않고 확인 요청

## 7. 금지사항

- 서버/DB 도입 금지 (localStorage만 사용)
- 요청하지 않은 파일 생성 금지
- 더미 데이터 삽입 금지
- 한 번에 100줄 초과하는 코드 출력 금지

## 8. 커밋 규칙

- 기능 단위로 커밋 (하나의 커밋에 여러 기능 섞지 않기)
- 메시지 형식: `feat: 활동 등록 기능 구현`, `fix: 날짜 검증 버그 수정`, `style: 반응형 레이아웃 적용` 등
- 두 사람 모두 자신의 브랜치에서 커밋을 남길 것 (총 5회 이상, 역할 분담 증빙 목적)
- **16:40 이후 커밋은 인정되지 않음** — 마감 여유를 두고 병합·커밋 마무리

## 9. 현재 진행 상황

### 필수기능 (유초윤 · `feat/core`)
- [x] 활동 등록
- [x] 최신순 목록 조회
- [x] 삭제 (확인 포함)
- [x] 입력값 검증
- [x] 수정 기능
- [x] 기간 필터
- [x] (추가) 활동 유형 태그, 참여자 명단, 회비 사용 금액, 중복 등록 확인

### 차별화기능 (신찬영 · `feat/ui`)
- [ ] 반응형 레이아웃
- [ ] 검색 기능
- [ ] 통계 차트

### 병합
- [ ] 1차 병합 (70분 시점)
- [ ] 2차 최종 병합 (115분 시점)
