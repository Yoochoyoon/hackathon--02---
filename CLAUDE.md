# 프로젝트 개요
동아리 활동 기록 관리 웹앱.
동아리 임원이 활동 내역을 등록하고 조회·삭제할 수 있다.

팀: 02
팀원: <팀원 이름과 역할 — 제출 전 반드시 채울 것>

# 기술 스택
- HTML / CSS / JavaScript (프레임워크 없음)
- 데이터 저장: 브라우저 localStorage
- 외부 라이브러리: 없음

# 파일 구조
- index.html : 화면 구조
- style.css  : 스타일
- app.js     : 기능 로직
- docs/작업기록.md : 프롬프트·오류 기록 (보고서 3장 원본)

# 코딩 규칙
- 함수와 변수 이름은 영어 camelCase, 의미가 드러나게 작성
- 주요 함수 위에는 한 줄 한글 주석을 단다
- 하나의 함수는 하나의 일만 한다
- 들여쓰기는 스페이스 2칸

# 데이터 구조
활동 1건은 다음 형태로 저장한다.
{ id, title, date, place, memberCount, memo, createdAt }
localStorage 키: "activities"

# 작업 방식
- 코드를 작성하기 전에 무엇을 어떤 순서로 만들지 먼저 제안한다
- 한 번에 하나의 기능만 구현한다. 여러 기능을 동시에 만들지 않는다
- 기존에 동작하던 코드를 요청 없이 수정하거나 삭제하지 않는다
- 확실하지 않은 부분은 추측하지 말고 먼저 질문한다

# 기록 규칙
- 실제 입력한 프롬프트와 실제 발생한 오류를 `docs/작업기록.md`에 그때그때 남긴다.
  보고서 3장이 "실제 입력한 문장 그대로"를 요구하므로, 나중에 기억으로 복원하면
  틀리거나 꾸며진 내용이 된다.

# 하지 말 것
- 서버, 데이터베이스, 빌드 도구 도입 금지
- 요청하지 않은 파일 새로 생성 금지
- 사용하지 않는 예시 코드나 더미 데이터 대량 생성 금지
- 한 번에 100줄 넘는 코드를 통째로 출력하지 않기

# 커밋 규칙
- 기능 하나가 동작하면 바로 커밋한다
- 커밋 메시지 형식: <기능명>: <무엇을 했는지>
  예) 활동등록: 입력 폼과 저장 기능 구현

# 현재 진행 상황
- [ ] 활동 등록
- [ ] 활동 목록 조회
- [ ] 활동 삭제
- [ ] 입력값 검증
- [ ] 선택 기능: 키워드 검색
- [ ] 선택 기능: 반응형 레이아웃
