# 해커톤 실행 계획 — 역할 분담 & 타임라인

> 저장소: https://github.com/sung2ne/20260805
> 작성일: 2026-08-05

## 1. 과제 요약

- **과제**: 동아리 활동 기록 관리 웹앱 개발
- **기술 스택**: HTML / CSS / JavaScript (프레임워크 없음), 데이터 저장은 브라우저 `localStorage`, Claude Code 필수 사용
- **팀 인원**: 2명 (둘 다 Claude Code 사용)
- **일정**: 13:30–17:00 (총 210분)

| 구간 | 시간 |
|---|---|
| 기획 | 20분 |
| **구현** | **130분** |
| 마무리 | 30분 |
| 총평 | 20분 |

- **커밋 마감**: 16:40 이후 커밋은 인정하지 않음
- **제출물**: 공개 GitHub 저장소 1개(팀당), `CLAUDE.md` + `README.md` 포함, 커밋 5회 이상, 결과 보고서 PDF

### 필수 기능 (반드시 구현)
1. 활동 등록 (활동명, 날짜, 장소, 참여 인원, 메모)
2. 최신순 목록 조회
3. 삭제 기능 (확인 절차 포함)

### 입력값 검증
- 활동명 필수
- 날짜는 미래 날짜 입력 불가
- 참여 인원은 1 이상 정수

### 선택 기능 (차별화용, 시간 남으면)
검색, 기간 필터, 수정, 통계 차트, 데이터 내보내기/가져오기, 반응형 레이아웃 등

### 데이터 구조 (CLAUDE.md 템플릿 고정)
```
{ id, title, date, place, memberCount, memo, createdAt }
```

### 보고서 관련 핵심 유의사항
- 분량 2~4쪽, PDF 제출
- **3장 "Claude Code 활용 과정"이 보고서의 핵심** — 실제 입력 프롬프트를 **요약하지 말고 그대로** 옮길 것 (핵심 프롬프트 3건, 프롬프트 개선 사례 1건, 오류 해결 사례 1건 포함)
- 읽을 수 있는 크기의 스크린샷 포함
- 마지막에 "역할 분담과 소감" 섹션 필요

---

## 2. 역할 분담 원칙

바닐라 JS + 파일 3개(index.html, style.css, app.js) 구조에서 2명이 같은 파일을 동시에 건드리면 병합 충돌로 시간을 낭비하게 됩니다. 따라서:

1. 구현 시작 전 **데이터 구조·함수 시그니처만 짧게 합의**
2. 이후 **파일 소유권 기준**으로 나눠서 각자 Claude Code를 독립적으로 실행
3. 선택 기능은 `features.js`로 분리해 `<script>` 태그로 얹어, A의 핵심 로직 파일을 직접 건드리지 않도록 함

| | **A: 필수기능 담당** | **B: 차별화기능 + UI 담당** |
|---|---|---|
| 소유 파일 | `app.js` (핵심 CRUD 로직) | `index.html`, `style.css`, `features.js` |
| 구현 범위 | 등록·조회·삭제, 입력값 검증, localStorage 저장/로드 | 레이아웃, 반응형, 선택기능 1~2개, 디자인 |
| 우선순위 | 최우선 안정화 (없으면 제출 불가) | 시간 부족하면 컷 가능 |

---

## 3. 130분 구현 타임라인

| 시간 | 내용 | 담당 |
|---|---|---|
| 0–10분 | 기획 결과 반영, CLAUDE.md 최종화, 데이터 구조·함수 이름 합의(`addActivity()`, `renderList()`, `deleteActivity(id)` 등), 파일 스켈레톤 생성 후 첫 커밋 | 같이 |
| 10–70분 | 필수기능 3종(등록/조회/삭제) 완성 | A |
| 10–70분 | 레이아웃·CSS 뼈대, 선택기능 설계 및 착수 | B |
| 70–80분 | 1차 병합 + 통합 테스트, 필수기능 정상 동작 확인 후 커밋 | 같이 |
| 80–115분 | 버그 수정, 엣지케이스, 반응형 보완 | A |
| 80–115분 | 선택기능 마무리 (1~2개로 압축) | B |
| 115–130분 | 최종 병합, 전체 테스트, 커밋 5회 이상 확인, README 정리 | 같이 |

**115~130분 구간은 새 기능 추가 금지, 병합·정리·커밋 전용으로 고정** — 마감 직전 충돌이 나면 복구할 시간이 없음.

---

## 4. 마무리(30분) & 총평(20분)

- 한 명: 스크린샷 캡처, 코드 최종 정리, PDF 변환
- 다른 한 명: 보고서 3장(핵심 프롬프트 3건, 프롬프트 개선 사례, 오류 해결 사례) 초안 작성
- 총평: 발표/시연 역할도 미리 정해두기 (한 명 발표, 한 명 화면 시연 컨트롤)

---

## 5. 놓치기 쉬운 포인트

- **프롬프트 기록**: 보고서는 프롬프트를 "그대로" 요구함. 구현 중 각자 쓴 핵심 프롬프트를 그때그때 별도 메모(예: `PROMPTS.md`)에 복붙해둘 것 — 나중에 기억으로 재구성하면 부정확하고 시간도 더 걸림.
- **커밋 분산**: 보고서의 "역할 분담" 섹션 대비, 두 사람 모두 자기 작업에서 커밋이 남도록 할 것. 기여도 증빙 목적.
- **16:40 마감**: 시간 역산해서 여유 확보.
